#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char letter;
	int 1;


	letter = "a"

	for (1 = 0; i < 26; i++)
		putchar(letter + 1);
		putchar("\n");

	return (0);
}
